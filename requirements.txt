# Core scientific and data libraries
pandas>=1.3
numpy>=1.21
scikit-learn>=1.0
matplotlib>=3.4

# SQLite (included in Python stdlib, listed for clarity)
sqlite3

# Plotting and dashboarding
plotly>=5.3
dash>=2.0
jupyter-dash>=0.4

# Interactive widgets and tables
ipywidgets>=7.6
qgrid>=1.3

# Jupyter environment
notebook>=6.4
ipython>=7.26

# Optional: HTML rendering
beautifulsoup4>=4.10  # (Only needed if future parsing/HTML cleaning is planned)

# Optional: For compatibility (especially if deploying)
python-dotenv>=0.21  # Manage environment variables
